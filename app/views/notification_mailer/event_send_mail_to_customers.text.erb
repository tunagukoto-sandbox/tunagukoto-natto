<%= @event.event_select %>に参加申し込みの皆様
大変お世話になっております。
一般社団法人ツナグコト運営です。

この度は<%= @event.event_select %>に申し込みいただき誠にありがとうございます。
開催日程前日になりましたので改めてご連絡いたしました。

イベント詳細は下記になります
イベント名：<%= @event.event_select %>

タイトル：<%=  @event.event_name %>
		<%=  @event.event_title %>
		
登壇：　<%= @event.event_company_name %>/<%= @event.position %>/<%= @event.event_president %>

日時：<%= @event.event_time.month %>月<%= @event.event_time.day %>日 <%= @event.event_time_detail %>

場所：ツナグコトオフィス(千種駅２番)

住所：<%= @event.event_location %>

<% if @event.get_point != nil %>
取得ポイント<%= @event.get_point %>
<% end %>

<% if @event.pay_point != nil %>
消費ポイント<%= @event.pay_point %>
<% end %>


備考：当日は17:30頃から受付を開始いたします。

持ち物：筆記用具　（服装自由）


電車でのお越しの際は千種駅地下2番出口から直結になっております。

詳細は下記ホームページにてご確認ください。

http://tunagu.or.jp/access.htm





それでは皆様と当日お会いできることを楽しみにしております。